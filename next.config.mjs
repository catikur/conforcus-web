/** @type {import('next').NextConfig} */
const nextConfig = {
  // VPS'te Docker ile standalone çıktı (CLAUDE.md kararı)
  output: "standalone",
  reactStrictMode: true,
};

export default nextConfig;
